# Лабораторная работа №2

## 📝 Описание
Проект посвящен изучению и практическому применению стандартной библиотеки шаблонов (STL) в C++17. Реализованы задачи по манипуляции данными в различных типах контейнеров.

## ✅ Реализованный функционал
- **Работа с контейнерами:** Использование `std::vector`, `std::list` и `std::deque`.
- **Алгоритмы:** Применение `std::replace_copy_if`, `std::swap_ranges`, `std::rotate`, `std::partial_sort`.
- **Логика задач:**
  - Изменение элементов по индексам и итераторам (удвоение значений).
  - Интерлейсинг (переплетение) двух списков.
  - Поиск и вставка элементов по условию (работа с отрицательными и положительными числами).

## 🛠 Сборка и запуск
1. Создайте папку для сборки: `mkdir build && cd build`
2. Сконфигурируйте проект: `cmake ..`
3. Соберите проект: `cmake --build .`
4. Запуск: `./Lab1_Executable` (или `Lab1_Executable.exe` на Windows)
